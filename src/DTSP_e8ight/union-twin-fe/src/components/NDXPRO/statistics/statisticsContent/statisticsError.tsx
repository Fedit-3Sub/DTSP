function StatisticsError({ err }: { err?: any }) {
  // useEffect(() => {
  //   console.log(err);
  // }, [err]);
  return <div className="statistics-error">Error</div>;
}

export default StatisticsError;
